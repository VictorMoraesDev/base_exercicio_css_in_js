import styled from 'styled-components'

const HeroContainer = styled.div`
  // Adicione suas regras de estilo aqui
`

const HeroTitle = styled.h1`
  // Adicione suas regras de estilo aqui
`

const Hero = () => (
  <HeroContainer>
    <HeroTitle>Bem-vindo ao Hero</HeroTitle>
  </HeroContainer>
)

export default Hero
